# Ailand POS - Flutter收银台系统

一个专业的收银台管理系统，使用 Flutter 开发，支持 Android、macOS 平台。

## 功能特性

- 🌐 网络自动检测
- 🔐 用户登录/注册
- 💰 快速收银
- 🎁 礼品兑换
- 👥 顾客管理
- 📊 经营报表
- 💼 财务管理
- ⚙️ 系统设置
- 🌍 国际化支持（中文/英文）

## 技术栈

- **框架**: Flutter 3.x
- **状态管理**: GetX
- **UI 组件库**: Shadcn UI
- **网络请求**: Dio
- **本地存储**: SharedPreferences
- **屏幕适配**: flutter_screenutil
- **国际化**: flutter_localizations + intl

## 项目结构

```
lib/
├── main.dart                 # 应用入口
├── app/
│   ├── routes/              # 路由配置
│   ├── config/              # 应用配置
│   └── theme/               # 主题配置
├── core/
│   ├── constants/           # 常量定义
│   ├── network/             # 网络层（Dio、网络检测）
│   └── storage/             # 本地存储
├── data/
│   ├── models/              # 数据模型
│   ├── repositories/        # 数据仓库
│   └── services/            # API 服务
├── modules/
│   ├── network_check/       # 网络检测模块
│   ├── login/               # 登录模块
│   ├── home/                # 首页模块
│   └── components_demo/     # Shadcn UI 组件展示
├── shared/
│   ├── widgets/             # 共享组件
│   └── extensions/          # 扩展方法
└── l10n/                    # 国际化文件
```

## 运行项目

1. 确保已安装 Flutter SDK
2. 获取依赖：
   ```bash
   flutter pub get
   ```

3. 生成国际化文件：
   ```bash
   flutter gen-l10n
   ```

4. 运行项目：
   
   **Android 设备/模拟器：**
   ```bash
   flutter run
   ```
   
   **macOS 桌面：**
   ```bash
   flutter run -d macos
   ```

## 构建应用

**Android APK：**
```bash
flutter build apk --release
```

**macOS 应用：**
```bash
flutter build macos --release
```

## 配置说明

### API 地址配置

在 `lib/core/constants/app_constants.dart` 中修改 API 基础地址：

```dart
static const String baseUrl = 'https://your-api-url.com';
```

### 屏幕适配

设计稿尺寸为 1080x1920，已在 main.dart 中配置 ScreenUtil。

## 模块说明

### 网络检测模块 (network_check)
- 检测外网连接状态
- 检测中心服务器连接
- Ping 测试（外网、DNS、中心服务）

### 登录模块 (login)
- 手机号登录
- 验证码获取
- 新用户注册

### 首页模块 (home)
- 功能卡片网格
- 收银员信息展示
- 系统消息通知

### 组件展示模块 (components_demo)
- Shadcn UI 所有组件的使用示例
- 包括按钮、卡片、输入框、选择器、提示、徽章、对话框等
- 可作为开发参考

## UI 组件库 (Shadcn UI)

本项目使用 Shadcn UI 作为主要 UI 组件库，提供简洁、现代的界面风格。

### 查看组件示例

运行应用后，在首页点击右上角的"组件展示"按钮，可以查看所有可用组件的实际效果。

### 常用组件

- **ShadButton**: 按钮组件（主要、次要、轮廓、幽灵、危险等样式）
- **ShadCard**: 卡片组件
- **ShadInput**: 输入框组件
- **ShadSwitch**: 开关组件
- **ShadCheckbox**: 复选框组件
- **ShadSlider**: 滑块组件
- **ShadAlert**: 提示组件
- **ShadBadge**: 徽章组件
- **ShadDialog**: 对话框组件
- **ShadToast**: Toast 消息组件

详细使用说明请参考 [SHADCN_UI_GUIDE.md](./SHADCN_UI_GUIDE.md)

## 国际化

支持中文和英文，系统会自动根据设备语言切换。

添加新的翻译：
1. 在 `lib/l10n/app_en.arb` 中添加英文
2. 在 `lib/l10n/app_zh.arb` 中添加中文
3. 运行 `flutter gen-l10n` 重新生成

## 后续开发建议

1. 在 `lib/modules/` 下创建新模块
2. 每个模块包含 `views/`、`controllers/`、`widgets/` 子目录
3. 在 `lib/app/routes/app_routes.dart` 中添加路由
4. 使用 GetX 的依赖注入管理控制器

## License

Private Project
